commit c52b284a504109d03fdb1a4515dd20094bd7e619
Author: Maximiliano Curia <maxy@debian.org>
Date:   Mon Feb 10 15:51:56 2014 +0100

    DictFileEdict::doSearch leaks

diff --git a/lib/DictEdict/dictfileedict.cpp b/lib/DictEdict/dictfileedict.cpp
index a84ffe1..fe5348e 100644
--- a/lib/DictEdict/dictfileedict.cpp
+++ b/lib/DictEdict/dictfileedict.cpp
@@ -193,6 +193,8 @@ EntryList *DictFileEdict::doSearch( const DictQuery &query )
     results = new EntryList();
     results->appendList( common );
     results->appendList( uncommon );
+    delete common;
+    delete uncommon;
 
     EntryList *exact     = new EntryList();
     EntryList *beginning = new EntryList();
@@ -227,6 +229,10 @@ EntryList *DictFileEdict::doSearch( const DictQuery &query )
     results->appendList( beginning );
     results->appendList( ending );
     results->appendList( anywhere );
+    delete exact;
+    delete beginning;
+    delete ending;
+    delete anywhere;
   }
 
   return results;
